# Plan 3 — Hooks (guardrails)

> **Status:** STUB — implementation plan not yet written.
> **To draft this plan:** run `/writing-plans` against this file (after Plan 2 has shipped).

## Position in the build

| | |
|---|---|
| **This is plan** | 3 of 5 |
| **Prior plan** | [Plan 2 — MCP server](2026-05-05-kadai-02-mcp-server.md) — must be `DONE` |
| **Next plan** | [Plan 4 — Web viewer](2026-05-05-kadai-04-web-viewer.md) |
| **Index** | [README.md](README.md) |
| **Spec** | [`../specs/2026-05-05-kadai-design.md`](../specs/2026-05-05-kadai-design.md) |

## Goal

Install the Claude Code hooks that enforce the guardrails: `PreToolUse(Edit, Write)` blocks off-spine edits when no story is picked; `PostToolUse(Edit, Write)` captures edits to the picked story's changelog.

## Scope

- Implement the `PreToolUse(Edit, Write)` hook script
  - Reads `.kadai/.picked` and `[guardrail.allowed_paths]` from config
  - If no story picked AND target path is outside `.kadai/` AND not in the allowlist → exits non-zero with clear message
  - Honors `KADAI_BYPASS=1` env var (with audit logging to `.kadai/bypass.log`, including `KADAI_BYPASS_REASON` if set)
- Implement the `PostToolUse(Edit, Write)` hook script
  - If a story is picked AND `change_capture.enabled = true` → appends entry to that story's `changelog.md` (timestamp, file path, one-line summary)
- Add MCP tool `record_change(story_id, files, summary, commit?)` — the hook can call it, or write directly to changelog.md (decide during plan writing)
- Extend `kadai init` to install hook entries into `.claude/settings.json` (merged, not overwritten)
- Add config sections `[guardrail.allowed_paths]` and `[change_capture.enabled]` to `kadai init`'s default config.toml
- Tests: hook scripts return correct exit codes given various states (picked/unpicked, allowed/disallowed paths, bypass on/off)

## Testing approach (NOT a self-install)

Hook behavior is verified via:
- Direct unit tests of the hook scripts (exit codes given various states: picked/unpicked, allowed/disallowed paths, bypass on/off)
- Integration tests in temp-dir fixtures where `kadai init` is run against the temp dir and the hooks are exercised
- Optionally, a subagent spawned via the Agent tool given a temp-dir kadai workspace to exercise the guardrail end-to-end

**The kadai project repo itself stays clean** — no `.kadai/`, no installed hooks, no `.mcp.json`. We do NOT install kadai against this project at the end of Plan 3. (That happens only as the optional final wrap of Plan 5.)

## Out of scope (deferred)

- `UserPromptSubmit` hook — post-MVP
- `Stop` hook — post-MVP
- `kadai sync` git scraping — post-MVP

## Dependencies — what must exist before writing this plan

**Plan(s) shipped:** Plans 1, 2.

**Specifically, these inputs:**

- Plan 2's MCP server with all tools working
- `src/cli/init.ts` extended in Plan 2 to merge into `.mcp.json` (we now extend it again to merge into `.claude/settings.json`)
- The pick state file (`.kadai/.picked`) format from Plan 1
- Config schema from Plan 1 — extended with new sections `[guardrail]` and `[change_capture]`

## Outputs — what ships at the end of this plan

- Two working hook scripts installed by `kadai init` (in any project where it's run, NOT in this repo)
- `kadai init` merges into `.claude/settings.json` (verified via temp-dir tests)
- `KADAI_BYPASS=1` env var works as documented; `.kadai/bypass.log` accumulates entries
- All hook behaviors verified via subagent + temp-dir tests; the kadai repo itself remains uninstalled

## Spec sections covered

- §3 — config sections `[guardrail.allowed_paths]`, `[change_capture.enabled]`
- §4 — `KADAI_BYPASS` env var + `bypass.log`
- §5.2 — hook table (the two MVP hooks)
- §10 — partial (subagent + temp-dir testing pattern is established here; formal acceptance test is Plan 5)

## When to write this plan

After Plan 2 ships and the MCP surface is stable. Read the actual MCP tool implementations from Plan 2 to know what tools the hooks can call.

## Compaction recovery note

If a fresh Claude lands here:
1. Verify Plans 1 and 2 are `DONE` per [`README.md`](README.md).
2. Read spec §5.2 (hooks) and §3 (config sections).
3. Read Plan 2's MCP tools and decide whether `record_change` is a new MCP tool or a direct file append in the hook.
4. Run `/writing-plans` and reference this file.
5. **Do NOT install kadai against the kadai project** at the end of this plan — the dogfood install only happens (optionally) in Plan 5. Use temp dirs and subagents for testing instead.
